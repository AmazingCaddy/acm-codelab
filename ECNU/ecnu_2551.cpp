#include<stdio.h>
#include<string.h>
#define N 105
int main()
{
	char a[N],b[N],c[N];
	int t,i,j,k,l,p;
	scanf("%d",&t);
	for(i=0;i<t;i++)
	{
		scanf("%s%s",a,b);
		len1=strlen(a);
		len2=strlen(b);
		l=0;k=0;
		for(j=0;j<len2&&k<len1;j++)
		{
			if(b[j]!=a[k])c[l++]=b[j];
			else 
			{
				for(p=)
			}

		for(k=j;k<len2;k++)
			c[l++]=b[k];

		c[l]=0;

		for()

